import React, { Component } from 'react';

class CreateAppButtonLabel extends Component {
  render() {
    return (
      <label className="main-section-label"> Create a new Kubernetes application using the CaaScade language or a git repository  </label>
    );
  }
}

export default CreateAppButtonLabel;
