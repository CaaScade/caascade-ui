import React, { Component } from 'react';

class CreateAppButton extends Component {
	render() {
		return (
			<button className="main-section-button"> + Create New Application </button>
		);
	}
}

export default CreateAppButton;
